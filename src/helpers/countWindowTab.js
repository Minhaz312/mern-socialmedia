// let tabCount = 1;
// if (typeof(Storage) !== "undefined") {
//     // Define a unique identifier for your website (you can use your domain name)
//     let websiteIdentifier = "tubebook";
      
//     // Count the number of tabs/windows opened for the same domain
//     tabCount = JSON.parse(sessionStorage.getItem(websiteIdentifier).times);

//     // Display the tab count
//     console.log("Tabs/windows opened for the same domain: " + tabCount);
//   } else {
//     console.log("sessionStorage is not supported in this browser.");
//   }

// export default tabCount;